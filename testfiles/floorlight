//Initialize a single elevator.
@ = 1;
init_elevators([0]);

sleep(1000);

//Go to highest floor.
make_order(0, CAB, 3);

~assert(() bool {
  return floorlight(@) == floor(@);
}, 250);

await_or_exit(() bool {
  return floor(@) == 3;
}, 20000);
